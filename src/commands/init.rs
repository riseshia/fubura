pub struct InitCommand;

impl InitCommand {
    pub fn run() {
        println!("init called!")
        // check file exists
        // if yes, exit with warn
        // else write config files
    }
}
